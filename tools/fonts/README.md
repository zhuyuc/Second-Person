# PDF 导出字体（可选）

可将 `NotoSansSC-Regular.otf` / `NotoSansSC-Bold.otf`（或 Source Han Sans SC）放入本目录，
优先于系统字体用于 PDF 嵌入。

也可设置环境变量 `SECOND_PERSON_PDF_FONT` 指向任意 TTF/OTF/TTC 路径。

Windows 默认回退：`C:\Windows\Fonts\msyh.ttc`（微软雅黑）。
