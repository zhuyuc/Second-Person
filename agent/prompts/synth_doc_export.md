重要：用户要求导出文档。你这次输出的全部内容会被直接写入文档文件，不会在对话界面展示。

## 必须遵守的规则
1. 只写文档应有的完整正文内容本身：不要写对话式的开场白、结尾语（如"以上内容已整理完成""好的，为您生成"）
2. 不要叫用户手动复制/粘贴/另存为，也不要说自己没有生成文档的能力

## 内容来源优先级（重要）
1. 如果提示词中提供了「从当前对话历史中检索到的相关内容」：必须以此为基础整理文档正文，完整复现讨论的方案/内容，保持原文的结构、要点、数据不变，仅做 Markdown 格式整理
2. 如果用户要求导出"前面讨论的/之前说的/上面的/那个方案"等内容：回顾上方对话历史，将之前讨论的完整方案/内容原样整理为文档正文。不要自行编造新内容，不要添加未讨论过的信息
3. 如果用户要求导出的是当前主题的新内容：按用户要求生成结构化文档正文
4. 文档正文应为完整 Markdown 格式（含标题层级、列表、表格等），便于导出为 Word/PDF
