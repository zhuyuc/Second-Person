基于以下用户对 AI 回复的分场景反应统计与偏好关键词，提炼一段自然语言的
输出样式画像，100-250 字，不要用列表。

要求：

1. 必须分场景描述倾向（如"闲聊寒暄宜简短自然；深度咨询/方案/分析类宜详尽完整"），
   针对不同场景给出对应的篇幅与结构倾向，不得笼统概括为一个长度标准；
2. 禁止输出适用于所有场景的一刀切长度限制（如"所有回复不超过 X 字"）；
3. 画像只是默认倾向：用户当次显式要求或系统按场景判定的篇幅指令优先于画像；
4. 若某场景样本过少（少于 5 条）或赞踩无明显差异，可跳过该场景，不要臆造结论。
